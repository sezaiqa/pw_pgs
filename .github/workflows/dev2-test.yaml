name: Run Playwright Tests on Dev2

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        shard: [1/2, 2/2]  

    steps:
      # Cache node modules to speed up the dependency installation
      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm ci

      - name: Run Playwright tests
        run: |
          echo "Running shard ${{ matrix.shard }}"
          npx playwright test --config=playwright.config2.js --project=chromium --shard=${{ matrix.shard }}
